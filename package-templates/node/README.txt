Steps to run locally:
1) At the root folder, run "npm install" to install all the node modules.
2) Run "node app.js"
3) Open http://localhost:3000 on the browser

Steps to deploy:
1) Open the manifest.yml, and edit the appname or hostname if necessary
2) Run "cf push"
